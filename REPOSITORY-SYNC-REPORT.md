# تقرير مزامنة المستودعين

## النطاق

تمت مقارنة `ISM-dragon/-` مع `ISM-dragon/-1`. المستودع الأول هو تطبيق Android أصلي مبني بـ Kotlin وJetpack Compose وRoom وWorkManager وMedia3، بينما المستودع الثاني هو تطبيق Tauri/React مع Pipeline Python وGateway API. لم يتم استبدال Kotlin بل تم الإبقاء عليه كتطبيق الهاتف، ودمج Gateway/Python كخدمة خلفية اختيارية.

## سبب العطل في المستودع الأول

كان مسار اختيار الفيديو يمرر `content://` من مزود الملفات مباشرة إلى مهمة WorkManager. هذا النوع من Uri قد يعتمد على صلاحية مؤقتة أو عملية مزود الملفات، ثم يفشل عندما تبدأ المهمة بعد مغادرة الشاشة. كان المسار يضيف المهمة إلى الطابور، ثم لا يحصل على مشروع مكتمل؛ لذلك لا ينتقل إلى Studio بشكل موثوق.

كما أن Studio كان يعرض كل المقاطع القديمة كبديل إذا لم يجد مقاطع للمشروع الجديد، ما يجعل النتيجة تبدو وكأن التطبيق عاد إلى مسار سابق بدل إظهار حالة المشروع الفعلية.

## الإصلاحات المدمجة

- نسخ الفيديو المختار إلى مساحة التطبيق الخاصة قبل تشغيل WorkManager.
- تمرير Uri ثابت إلى Room وWorker وGemini وMedia3.
- منع Studio من عرض مقاطع مشروع آخر عند غياب نتائج المشروع المحدد.
- إضافة تبويب `Social Gateway` داخل تطبيق Kotlin.
- حفظ رابط Gateway ورمز الجلسة بتخزين مشفر، وعرض حالة الحسابات والمنشورات والطابور.
- دمج Gateway API ولوحة التحكم وSQLite والجدولة وسياسات الحدود والإيقاف الآمن.
- دمج توثيق Android وSocial Hub والتحسينات.
- تغيير الاسم الظاهر إلى `ISM` مع إبقاء معرف الحزمة ومسار البيانات ومفتاح Keystore الداخلي للتوافق.
- السماح باتصالات HTTP المحلية في Debug فقط، مع بقاء Release متطلبًا HTTPS خارج الشبكة المحلية.

## التحقق

- `./gradlew :app:assembleDebug`: ناجح.
- `./gradlew :app:testDebugUnitTest --tests com.example.PublishSafetyTest --tests com.example.PipelineWorkContractTest`: ناجح.
- `python3 -m py_compile gateway/main.py`: ناجح.
- فحص عقد Gateway للمسارات الأساسية: ناجح.
- الاختبارات الكاملة القديمة التي تعتمد على Robolectric/لقطات الشاشة بقيت بطيئة أو معلقة في بيئة Sandbox؛ تحتاج CI أو جهاز Android/Emulator يدعم KVM.

## حدود معروفة

النشر الحقيقي إلى Instagram وFacebook وTikTok وYouTube وX يحتاج مفاتيح OAuth الرسمية وموفري رفع الوسائط داخل Gateway. النسخة المدمجة توفر العقد واللوحة ووضع Mock للاختبار ولا تضع أسرار المنصات داخل APK.
